This repo will contain kernel sources for LGE G2

For build this kernel:
	-Edit build script (build.sh):
		-Change toolchains location 
		-Change name of make .zip file (Opcional)
	-Add your ramdisk in root folder
	-Add META-INF & opcional folders/files in build folder
	-Open a terminal & execute: sh build.sh or . build.sh
